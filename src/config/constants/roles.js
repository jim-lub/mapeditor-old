const GUEST = {
  permissions: {
    'canSignUp': true,
    'canSignIn': true,
    'canForgotPassword': true
  }
}

const MEMBER = {
  'canSignOut': true,
  'canChangePassword': true
}

const ADMIN = {
  'canSignOut': true,
  'canChangePassword': true
}
